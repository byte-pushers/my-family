language: bash
sudo: required
services:
  - docker
deploy:
  - edge:
      branch: true
  - provider: script
    skip_cleanup: true
    script:
      - make dockerpush
  - on:
      branches:
        - master
        - feature/dockerize-app
notifications:
  email:
    recipients:
      - tonte.pouncil@bytepushers.software
    on_success: always
    on_failure: always