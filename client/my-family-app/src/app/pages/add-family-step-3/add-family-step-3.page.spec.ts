import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AddFamilyStep3Page } from './add-family-step-3.page';

describe('AddFamilyStep3Page', () => {
  let component: AddFamilyStep3Page;
  let fixture: ComponentFixture<AddFamilyStep3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AddFamilyStep3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
